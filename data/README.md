# Dataset availability

The evaluation set contains 842 aspect annotations across 389 Roman Urdu reviews. Raw review text is not redistributed here because the current file does not preserve per-row provenance for Kaggle, directly collected, and supplementary examples.

To reproduce the evaluation, place an authorized copy at `daraz_dataset.csv`. Expected columns: `review_id`, `txt`, `span`, and `label`.

